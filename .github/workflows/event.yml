name: First Workflow
on:
  pull_request:
    types:
      - opened
  workflow_dispatch:
  push:
    branches: 
      - main
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello world"
      - name: Print goodbye
        run: echo "Byeee!!"
        
